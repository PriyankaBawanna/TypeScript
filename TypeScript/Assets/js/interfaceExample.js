var OperatingSystem = function (type) {
  console.log("Android " + type.name + " has " + type.language + " language.");
};
var Oreo = { name: "O.S", language: "Java" };
OperatingSystem(Oreo);
console.log("this is java  script interface");
